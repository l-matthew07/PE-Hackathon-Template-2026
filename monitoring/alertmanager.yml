global:
  resolve_timeout: 5m

route:
  receiver: discord
  group_by: [alertname, instance]
  group_wait: 0s
  group_interval: 10s
  repeat_interval: 10m

receivers:
  - name: discord
    webhook_configs:
      - url: "http://discord-relay:9094"
        send_resolved: true
